[package]
name = "system76_firmware_smmstore"
version = "1.0.0"
edition = "2018"
license = "GPL-3.0-only"

[profile.release]
lto = true

[dependencies]
redox_uefi = { git = "https://gitlab.redox-os.org/tcrawford/uefi.git", branch = "fvb" }
redox_uefi_std = { version = "0.1.4", optional = true }
rlibc = "1.0"
plain = "0.2"

[features]
default = ["redox_uefi_std"]
